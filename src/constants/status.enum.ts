export enum JobStatus {
  OPEN = 'open',
  ARCHIVED = 'archived',
}

export enum ApplicationStatus {
  PENDING = 'pending',
  REVIEWING = 'reviewing',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
